<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notifications | Admin Panel</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/sidebar.css" rel="stylesheet">
    <style>
        :root {
            --primary: #2D3250;
            --secondary: #424769;
            --accent: #676F9D;
            --light: #F9F9F9;
            --dark: #1A1B26;
            --success: #4CAF50;
            --warning: #FF9800;
            --error: #F44336;
            --info: #2196F3;
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            min-height: 100vh;
        }
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }
        .topbar {
            background: white;
            padding: 1rem 2rem;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark);
            margin: 0;
        }
        .add-btn {
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }
        .add-btn:hover {
            background: var(--secondary);
            color: #fff;
            transform: translateY(-1px);
        }
        .content-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .notification-item {
            border-bottom: 1px solid #f0f0f0;
            padding: 1.5rem;
            transition: background-color 0.2s;
        }
        .notification-item:hover {
            background-color: #f8f9fa;
        }
        .notification-item:last-child {
            border-bottom: none;
        }
        .notification-header {
            display: flex;
            justify-content: between;
            align-items: flex-start;
            margin-bottom: 0.5rem;
        }
        .notification-title {
            font-weight: 600;
            color: var(--dark);
            margin: 0;
            flex: 1;
        }
        .notification-message {
            color: #666;
            margin-bottom: 1rem;
            line-height: 1.5;
        }
        .notification-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.875rem;
            color: #888;
        }
        .notification-actions {
            display: flex;
            gap: 0.5rem;
        }
        .btn-sm {
            padding: 0.25rem 0.75rem;
            font-size: 0.875rem;
        }
        .status-toggle {
            cursor: pointer;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.75rem;
            font-weight: 500;
        }
        .status-active {
            background: rgba(76, 175, 80, 0.1);
            color: var(--success);
        }
        .status-inactive {
            background: rgba(158, 158, 158, 0.1);
            color: #666;
        }
        .pagination {
            justify-content: center;
            margin-top: 2rem;
        }
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: #666;
        }
        .empty-state svg {
            width: 64px;
            height: 64px;
            margin-bottom: 1rem;
            opacity: 0.5;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="sidebar">
        <%- include('../partials/sidebar', { 
            activePage: 'notifications', 
            username: username,
            activeCategory: activeCategory,
            activeSubCategory: activeSubCategory,
            koshCategories: koshCategories,
            mcqCategories: mcqCategories
        }) %>
    </nav>
    
    <main class="main-content">
        <div class="topbar">
            <h1 class="page-title">Notifications</h1>
            <a href="/notifications/add" class="add-btn">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                </svg>
                Add Notification
            </a>
        </div>


        <div class="content-card">
            <% if (notifications && notifications.length > 0) { %>
                <% notifications.forEach(function(notification) { %>
                    <div class="notification-item">
                        <div class="notification-header">
                            <h3 class="notification-title"><%= notification.title %></h3>
                        </div>
                        <div class="notification-message"><%= notification.message %></div>
                        <div class="notification-meta">
                            <div>
                                <span>Created: <%= new Date(notification.createdAt).toLocaleDateString() %></span>
                                <% if (notification.scheduledAt) { %>
                                    <span class="ms-3">Scheduled: <%= new Date(notification.scheduledAt).toLocaleDateString() %></span>
                                <% } %>
                                <% if (notification.expiresAt) { %>
                                    <span class="ms-3">Expires: <%= new Date(notification.expiresAt).toLocaleDateString() %></span>
                                <% } %>
                                <span class="ms-3">Read: <%= notification.readCount || 0 %></span>
                            </div>
                            <div class="notification-actions">
                                <span class="status-toggle status-<%= notification.isActive ? 'active' : 'inactive' %>" 
                                      onclick="toggleStatus('<%= notification._id %>')">
                                    <%= notification.isActive ? 'Active' : 'Inactive' %>
                                </span>
                                <a href="/notifications/edit/<%= notification._id %>" class="btn btn-sm btn-outline-primary">Edit</a>
                                <form action="/notifications/delete/<%= notification._id %>" method="POST" class="d-inline" 
                                      onsubmit="return confirm('Are you sure you want to delete this notification?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }); %>
                
                <!-- Pagination -->
                <% if (totalPages > 1) { %>
                    <nav aria-label="Page navigation" class="pagination">
                        <ul class="pagination">
                            <% if (currentPage > 1) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= currentPage - 1 %>">Previous</a>
                                </li>
                            <% } %>
                            
                            <% for (let i = 1; i <= totalPages; i++) { %>
                                <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                                    <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                                </li>
                            <% } %>
                            
                            <% if (currentPage < totalPages) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= currentPage + 1 %>">Next</a>
                                </li>
                            <% } %>
                        </ul>
                    </nav>
                <% } %>
            <% } else { %>
                <div class="empty-state">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M15 17h5l-5 5-5-5h5v-5a7.5 7.5 0 0 0-15 0v5h5l-5 5-5-5h5v-5a7.5 7.5 0 0 0 15 0v5z" />
                    </svg>
                    <h3>No notifications found</h3>
                    <p>Create your first notification to get started.</p>
                    <a href="/notifications/add" class="add-btn">Add Notification</a>
                </div>
            <% } %>
        </div>
    </main>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
        function toggleStatus(notificationId) {
            fetch(`/notifications/toggle/${notificationId}`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                } else {
                    alert('Error toggling notification status');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Error toggling notification status');
            });
        }
    </script>
</body>
</html>
