<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Karmkand Subcategory | Sanskrit Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/sidebar.css" rel="stylesheet">
    <style>
        :root {
            --primary: #2D3250;
            --secondary: #424769;
            --accent: #676F9D;
            --light: #F9F9F9;
            --dark: #1A1B26;
            --success: #4CAF50;
            --error: #F44336;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            min-height: 100vh;
        }
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }
        .topbar {
            background: white;
            padding: 1rem 2rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark);
            margin: 0;
        }
        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }
        .content-card {
            background: white;
            border-radius: 16px;
            box-shadow: var(--shadow);
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        .form-label {
            font-weight: 500;
            color: var(--dark);
            margin-bottom: 0.5rem;
        }
        .form-control {
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
        }
        .form-control:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(45, 50, 80, 0.1);
        }
        .btn-submit {
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-submit:hover {
            background: var(--secondary);
            transform: translateY(-1px);
            box-shadow: var(--shadow);
        }
        .btn-cancel {
            background: white;
            color: var(--secondary);
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-cancel:hover {
            background: rgba(0, 0, 0, 0.05);
            color: var(--primary);
        }
        .alert {
            border: none;
            border-radius: 12px;
            padding: 1rem 1.5rem;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        .alert-danger {
            background: rgba(244, 67, 54, 0.1);
            color: var(--error);
        }
        .parent-category {
            background: rgba(103, 111, 157, 0.1);
            padding: 1rem;
            border-radius: 12px;
            margin-bottom: 1.5rem;
        }
        .parent-category-label {
            font-size: 0.875rem;
            color: var(--secondary);
            margin-bottom: 0.25rem;
        }
        .parent-category-name {
            font-weight: 600;
            color: var(--dark);
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="sidebar">
        <%- include('partials/sidebar', { activePage: 'karmkand-subcategories', activeCategory: parent._id, activeSubCategory: subcategory._id }) %>
    </nav>
    <!-- Main Content -->
    <main class="main-content">
        <!-- Topbar -->
        <div class="topbar">
            <h1 class="page-title">Edit Karmkand Subcategory</h1>
            <div class="user-menu">
                <div class="user-avatar">
                    <%= username ? username.charAt(0).toUpperCase() : 'A' %>
                </div>
            </div>
        </div>
        <% if (error) { %>
            <div class="alert alert-danger">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
                <%= error %>
            </div>
        <% } %>
        <!-- Content Card -->
        <div class="content-card">
            <div class="parent-category">
                <div class="parent-category-label">Parent Category</div>
                <div class="parent-category-name"><%= parent.name %></div>
            </div>
            <form action="/karmkand-subcategory/<%= subcategory._id %>/edit" method="POST">
                <div class="mb-4">
                    <label for="name" class="form-label">Subcategory Name</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= subcategory.name %>" required>
                </div>
                <div class="mb-4">
                    <label for="position" class="form-label">Position</label>
                    <input type="number" class="form-control" id="position" name="position" value="<%= subcategory.position %>" required>
                </div>
                <div class="mb-4">
                    <label for="introduction" class="form-label">Introduction</label>
                    <textarea class="form-control" id="introduction" name="introduction" rows="4"><%= subcategory.introduction %></textarea>
                </div>
                <div class="mb-4">
                    <label for="cover_image" class="form-label">Cover Image URL</label>
                    <input type="url" class="form-control" id="cover_image" name="cover_image" value="<%= subcategory.cover_image || '' %>" placeholder="https://example.com/image.jpg">
                </div>
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-submit">Update Subcategory</button>
                    <a href="/karmkand-subcategories?category=<%= parent._id %>" class="btn btn-cancel">Cancel</a>
                </div>
            </form>
        </div>
    </main>
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html> 